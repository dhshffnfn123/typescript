{
  "compilerOptions": {
    "outDir": "./dist", // dist 폴더에 생성된 자바스크립트 파일을 저장
    "noEmitOnError": true, // 모든 오류가 고쳐진 후에야 자바스크립트 파일이 생성
    "lib": ["dom", "ES6"] // 해당 라이브러리를 추가해야 tsc가 console.log 같은 웹 API를 알 수 없는 API로 인식하지 않음
  }
}
